---
// Astro Server Island component on a static/prerendered page breaks
// if there is not at least one dynamic route (not necessarily the page
// where the server component is used).
export const prerender = false;
---

<!-- no-op -->
